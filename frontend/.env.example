# Frontend environment variables (example)
# Copy this file to ".env" and adjust if your backend runs elsewhere.

# Base URL of the backend API. Must match the backend PORT.
VITE_API_URL=http://localhost:5001

# The person this assistant serves (shown in the UI greeting + header).
VITE_USER_NAME=Sreeja
